//default argument - a constant expression assigned to a paramerer
//at the end of the parameter list which is implicitly passed if
//it is not explicitly passed by the caller 
double BoxVolume(float length, float breadth, float height, float thickness = 1);
